{
  "name": "base-nfts-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ base-nfts-subgraph",
    "create-local": "graph create --node http://localhost:8020/ base-nfts-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ base-nfts-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 base-nfts-subgraph",
    "test": "graph test",
    "deploy-mainnet": "goldsky subgraph deploy showtime/$VERSION_TAG",
    "deploy-testnet": "goldsky subgraph deploy showtime-testnet/$VERSION_TAG"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.57.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
